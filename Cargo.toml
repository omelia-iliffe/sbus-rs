[package]
name = "sbus-rs"
version = "0.1.0"
edition = "2021"

[dependencies]
heapless = { version = "0.8.0" }
embedded-io = { version = "0.6.1", optional = true }

[dev-dependencies]
criterion = "0.5"
hex-literal = "0.4"

[features]
embedded-io = ["dep:embedded-io"]
std = []
default = []

[[bench]]
name = "benchmarks"
harness = false